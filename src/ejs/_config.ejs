<%
const getConfig = () => {
  const timestamp = Date.now()
  const publishRoot = path.dir.project
  const assetsDir = publishRoot + path.dir.distAssets
  const dir = {
    root: `${publishRoot}/`,
    css: `${assetsDir + path.dir.css}/`,
    js: `${assetsDir + path.dir.js}/`,
    img: `${assetsDir + path.dir.img}/`
  }

  const domain = 'https://hogehoge.hoge'

  const site = {
    root: `${domain + publishRoot}/`,
    title: 'タイトル',
    description: '説明',
    keywords: 'キーワード',
    copyright: 'コピーライト',
    ogp: `${domain + dir.img}ogp.png`,
    ogpDescription: 'OGP説明',
  }

  return {
    isProduction,
    timestamp,
    dir,
    site
  }
}
config = getConfig()
%>
